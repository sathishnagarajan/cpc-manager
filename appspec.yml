version: 0.0
os: linux
files:
  - source: /api/
    destination: /var/www/cpc-manager/dev/releases/latest
hooks:
  BeforeInstall:
    - location: scripts/cleanup-dev.sh
      timeout: 120
      runas: root
  AfterInstall:
    - location: scripts/composer-dev.sh
      timeout: 300
      runas: ec2-user
  ApplicationStart:
    - location: scripts/activate-dev.sh
      timeout: 120
      runas: root
  ValidateService:
    - location: scripts/healthcheck-dev.sh
      timeout: 60
      runas: ec2-user
